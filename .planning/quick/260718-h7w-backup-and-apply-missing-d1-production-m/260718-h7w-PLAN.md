---
phase: quick-260718-h7w-backup-and-apply-missing-d1-production-m
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - QUICK-D1-MIGRATION-RECOVERY
must_haves:
  truths:
    - 'All three production D1 shards have read-only schema, integrity, and critical-data baselines before DDL.'
    - 'All three plaintext exports are stored outside the repository with restrictive ACLs, hashes, and successful isolated local-import proof before the first DDL statement.'
    - 'Only schema-missing 0015 and 0016 files run, in numeric order, one shard at a time, with first-failure stop.'
    - 'Migration 0016 preserves every copied pengaturan field under a non-disclosing canonical expected-post hash and exact schema/index/FK checks.'
    - 'Concurrency is either quiesced or explicitly detected; append traffic is never misreported as migration data loss or atomic-snapshot proof.'
    - 'Authenticated smoke persists each transaction ID before further work and proves successful deletion plus zero transaction and ledger residue.'
    - 'Production service-worker hash remains identical to the deployed artifact.'
  artifacts:
    - path: '<absolute-recovery-dir>/initial/zatiaras-samarinda-group.sql'
      provides: 'Restrictive-ACL recoverable pre-DDL Samarinda export'
    - path: '<absolute-recovery-dir>/initial/zatiaras-balikpapan-group.sql'
      provides: 'Restrictive-ACL recoverable pre-DDL Balikpapan export'
    - path: '<absolute-recovery-dir>/initial/zatiaras-berau-group.sql'
      provides: 'Restrictive-ACL recoverable pre-DDL Berau export'
    - path: '<absolute-recovery-dir>/wrangler-safe-wrapper.mjs'
      provides: 'Secret-safe allowlisted Wrangler process boundary used by every remote command'
    - path: '<absolute-recovery-dir>/uat-fail-closed.mjs'
      provides: 'External production smoke harness with durable transaction journal and residue proof'
    - path: '.planning/quick/260718-h7w-backup-and-apply-missing-d1-production-m/260718-h7w-SUMMARY.md'
      provides: 'Sanitized backup, migration, concurrency, preservation, and live-smoke evidence'
  key_links:
    - from: 'wrangler.pages.jsonc'
      to: 'three production D1 targets'
      via: 'allowlisted database_name/database_id pairs passed through the secret-safe wrapper'
      pattern: 'zatiaras-(samarinda|balikpapan|berau)-group'
    - from: 'drizzle/meta/_journal.json'
      to: 'drizzle/0015_session_page_unlocks.sql and drizzle/0016_pin_hash.sql'
      via: 'missing-only journal order 15 then 16'
      pattern: '001(5_session_page_unlocks|6_pin_hash)'
    - from: 'drizzle/0015_session_page_unlocks.sql'
      to: 'src/lib/server/sessionStore.ts'
      via: 'session INSERT/SELECT requires unlocked_pages and unlock_expires_at'
      pattern: 'unlocked_pages|unlock_expires_at'
    - from: '<absolute-recovery-dir>/uat-fail-closed.mjs'
      to: '/api/pos/transaction and /api/transaksi-kasir'
      via: 'persist transaction ID, cleanup DELETE, then authenticated zero-residue queries'
      pattern: 'transaction_id'
---

<objective>
Back up and repair schema drift on all three authorized production D1 shards, then prove production behavior without exposing secrets, overstating snapshot guarantees, or leaving UAT financial residue.

Purpose: Restore login safely while preserving a usable recovery point and data-integrity evidence for the non-idempotent 0016 table rebuild.
Output: Retained encrypted-at-rest-by-host-access-control plaintext exports outside the worktree plus one sanitized `260718-h7w-SUMMARY.md`; no source/config edit, commit, build, or deployment.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260718-esb-deploy-production-and-run-live-smoke-tes/260718-esb-SUMMARY.md
@wrangler.pages.jsonc
@drizzle/meta/_journal.json
@drizzle/0015_session_page_unlocks.sql
@drizzle/0016_pin_hash.sql
@package.json
@scripts/uat-live-realtime.mjs
@scripts/uat-live-ui-two-device.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish secret-safe command boundary and prove all recovery exports</name>
  <files>&lt;absolute-recovery-dir&gt;/wrangler-safe-wrapper.mjs, &lt;absolute-recovery-dir&gt;/initial/*.sql</files>
  <action>
Capture UTC run ID, branch/HEAD, and initial `rtk git status --short --branch`. Create a unique persistent recovery directory outside every repository/worktree, under an absolute per-user application-data path rather than the OS disposable temp directory. Record its resolved absolute path. Remove inherited ACLs and grant access only to the current user and SYSTEM; verify the effective ACL before writing plaintext exports. Never add a repo backup path or edit `.gitignore`.

Create a temporary Node process wrapper inside that restrictive directory. It must parse `.env` into the child environment without logging names/values, reject shell interpolation, invoke argv through `rtk pnpm exec wrangler` with `shell: false`, allowlist only the three database names/IDs from `wrangler.pages.jsonc`, require the explicit config path, and require `--remote` for every production operation. It must capture stdout/stderr in memory, fail if any loaded secret value appears, redact before emitting diagnostics, and support writing sensitive JSON stdout directly to an ACL-protected file while printing only exit status/path/hash. Invoke the wrapper only through an RTK-prefixed shell command such as `rtk node <absolute-wrapper> ...`; do not call raw `wrangler`, `npx`, `pnpm exec wrangler`, or `scripts/d1-backup.mjs`. Prove the wrapper with secret-safe `whoami`/read-only metadata calls and record only expected account identity, never credentials.

Inspect code/config for a maintenance/read-only switch using RTK-prefixed search. Current evidence shows none; confirm that result. Before mutation, use the wrapper for read-only remote schema/baseline queries on Samarinda, Balikpapan, and Berau. Classify 0015 as applied only if both session columns exist, missing only if both do not, partial otherwise. Classify 0016 as applied only if `pin_hash` and `idx_pengaturan_branch` exist and `__new_pengaturan` does not, missing only if `pin_hash`/temporary table do not exist, partial otherwise. Stop on partial/ambiguous state. Capture sanitized counts and financial aggregates without row values or credentials.

Before the first DDL, export all three shards directly with the wrapper using `d1 export <database> --config wrangler.pages.jsonc --remote --skip-confirmation --output <absolute-recovery-file>`. Require exactly three non-empty files in `<absolute-recovery-dir>/initial`, restrictive inherited ACLs, absolute paths, byte sizes, and SHA-256 values; never print contents. Import every export into a separate isolated local `--persist-to` directory through the same wrapper/RTK boundary, run local integrity/schema/count checks, and require critical counts/financial aggregates to match the source baseline. Remove only disposable local-import state. Retain all plaintext exports and their manifest for recovery. No production DDL may begin until every export passes.
</action>
<verify>
<automated>The RTK-invoked wrapper proves Cloudflare identity without a secret leak; all three shards have unambiguous schema status; three outside-worktree exports have restrictive ACLs, non-zero size, SHA-256, and successful isolated local import with matching critical baseline.</automated>
</verify>
<done>Every remote Wrangler command has one proven secret-safe RTK process boundary, and all three recoverable backups are proven before first mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Apply missing-only DDL with concurrency-aware preservation gates</name>
  <files>&lt;absolute-recovery-dir&gt;/pre-ddl/*.sql, &lt;absolute-recovery-dir&gt;/evidence/*.json</files>
  <action>
Process Samarinda, Balikpapan, then Berau sequentially. Do not use `wrangler d1 migrations apply`. Because code/config has no enforceable maintenance/read-only switch, do not claim traffic quiescence or an atomic multi-command snapshot. Before each shard, prefer a short operationally quiet window: poll sanitized request metrics for no POST/PATCH/DELETE activity across every branch sharing that shard for at least 60 seconds. This is observational, not enforcement. If a quiet window cannot be observed, stop before that shard's DDL and report that an external maintenance window is required.

Immediately before each shard's DDL, reclassify schema, create an additional direct remote export at `<absolute-recovery-dir>/pre-ddl/<shard>-<utc>.sql`, verify ACL/non-zero/hash, and capture an adjacent baseline through secret-safe files. Minimize delay between this baseline and DDL. Baseline schema and critical counts/aggregates for `profil`, `auth_sessions`, `pengaturan`, `produk`, `sesi_toko`, `buku_kas`, and `transaksi_kasir`. For 0016, query every copied `pengaturan` field (`id`, `cabang_id`, `pin`, normalized `pin_hash`, `halaman_terkunci`, `nama_toko`, `alamat`, `telepon`, `instagram`, `ucapan`, `created_at`, `updated_at`) only into a restrictive evidence file. A local helper must canonicalize rows ordered by `(cabang_id,id)` using stable typed, length-prefixed UTF-8 values and emit digests only. Compute: (a) exact pre-state hash; and (b) expected-post hash that applies precisely the migration projection (`pin='1234'` becomes null, `pin_hash` becomes null when 0016 was missing, every other field byte-preserved). Never output raw fields.

Build the missing list only from 0015 and 0016, sorted numerically. Execute each exact checked-in file through the RTK-invoked secret-safe wrapper. Never reapply a present migration. After 0015 require exact column names/types/not-null/defaults, zero null unlock fields, and preservation evidence for rows in the immediate baseline. After 0016 require the exact ordered `PRAGMA table_info(pengaturan)` shape declared by the SQL, exactly the expected `idx_pengaturan_branch` definition from `PRAGMA index_list/index_info`, the expected empty `PRAGMA foreign_key_list(pengaturan)`, no `__new_pengaturan`, zero remaining raw default `1234` rows, and an actual canonical all-field hash exactly equal to the expected-post hash.

Run remote `PRAGMA quick_check`. If unavailable, try remote `PRAGMA integrity_check`; if both are unsupported, immediately export the post-DDL shard to a restricted temporary file, import it into isolated local D1, and require local `PRAGMA quick_check`, exact schema/index/FK assertions, canonical `pengaturan` digest, and critical query success. Delete only this post-check copy after proof.

Concurrency rules are fail-closed. Migrations touch only `auth_sessions`/`pengaturan`; unrelated append-table increases may be legitimate traffic, not migration damage. Compare timestamps/count/sum deltas and matching audit/request records without exposing row data. If any activity occurred during the interval, label global before/after count proof non-atomic. Allow an unrelated append increase only when completely accounted for by same-window production audit/transaction evidence; never describe it as unchanged snapshot. Any decrease, unexplained delta, changed `pengaturan` canonical hash, schema mismatch, integrity failure, partial DDL, or detected concurrent settings/session mutation makes preservation inconclusive: stop immediately, do not touch the next shard, do not auto-restore/retry, and do not run smoke. Record the shard, migration, exact recovery export path/hash, and limitation.
</action>
<verify>
<automated>All three shards finish with 0015/0016 applied or skipped-present in order; every executed DDL used the secret-safe wrapper; exact session and pengaturan schema/index/FK assertions pass; canonical expected-post pengaturan hashes match; integrity proof passes; every count delta is unchanged or explicitly attributable without false atomicity claims.</automated>
</verify>
<done>Missing-only migrations are verified on all three shards with first-failure stop, strong 0016 field preservation, and an honest concurrency classification.</done>
</task>

<task type="auto">
  <name>Task 3: Run fail-closed authenticated smoke, cleanup proof, and artifact verification</name>
  <files>&lt;absolute-recovery-dir&gt;/uat-fail-closed.mjs, .planning/quick/260718-h7w-backup-and-apply-missing-d1-production-m/260718-h7w-SUMMARY.md</files>
  <action>
Run only after Task 2 succeeds on all shards. Do not invoke the existing mutating UAT scripts directly because their final cleanup paths suppress errors. Build a one-off Node harness in the restrictive outside-worktree run directory, using the existing live scripts only as behavior references. Syntax-check it with `rtk node --check`. It must never log `UAT_PASSWORD`, cookies, CSRF/session tokens, or response headers.

The harness must perform production login, unauthenticated 401 probes, authenticated cross-branch 403, and both direct-write 409 guards. For the live checkout it must open two realtime clients, create a quote/transaction, then synchronously/atomically persist the returned transaction ID and branch to `<absolute-recovery-dir>/uat-transaction-journal.json` before awaiting realtime or doing further assertions. Cleanup is mandatory in `finally` and must not catch-and-ignore: obtain fresh owner CSRF/session state, require successful DELETE `/api/transaksi-kasir?transaction_id=<exact-id>`, then authenticated GETs to both `/api/transaksi-kasir?transaction_id=<exact-id>` and `/api/buku-kas?transaction_id=<exact-id>` must each prove zero rows. Only after both zero-residue queries pass may the journal be marked `cleaned`. On process restart or any journal with `cleaned != true`, cleanup-only mode must run before another transaction. Any cleanup or residue failure exits non-zero and blocks further testing.

For browser coverage, create an outside-worktree fail-closed adaptation of the two-device CDP harness with the same durable journal, mandatory DELETE response assertion, and authenticated zero-residue queries. Syntax-check before use. Run when Chrome/CDP is available; if launch is denied before transaction creation, record the exact limitation. If a transaction ID was ever journaled, browser failure cannot be downgraded to a limitation until cleanup and zero residue are proven.

Verify realtime Worker `/health`, public 200s, unauthenticated 401s, login 200, cross-branch 403, direct-write 409s, live quote/checkout, two realtime clients, and cleanup. Download production `/sw.js` to a restrictive temporary file with cache busting; require SHA-256 `b51c327e0d5b812b0897709c31ff6d7cee9ae96ce79d8273176266437bad7856` and local artifact equality when present, then remove the download.

Create only the repo summary `260718-h7w-SUMMARY.md`. Include sanitized absolute backup paths, ACL proof, hashes/local-import proof, wrapper proof, maintenance-switch search result, concurrency limitation/deltas, migration dispositions, exact schema/index/FK and canonical-hash outcomes, integrity fallback used, 200/401/403/409 matrix, transaction journal/DELETE/zero-residue proof, realtime/browser results, and service-worker hash. Do not commit. Retain recovery exports and manifest outside the repo; remove only disposable evidence containing row data after digesting it.
</action>
<verify>
<automated>The outside-worktree harness returns login 200, cross-branch 403, direct-write 409/409, two realtime clients, successful DELETE, and authenticated zero rows in transaction and ledger endpoints; any journal is `cleaned: true`; Worker health and deployed `sw.js` hash pass; final `rtk git diff --check` exits 0.</automated>
</verify>
<done>Full production smoke passes with fail-closed cleanup and zero residue, browser status is honest, artifact identity remains exact, and no source/config edit or commit occurred.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                    |
| ----------------------------------- | ---------------------------------------------------------------------------------------------- |
| `.env` to Cloudflare child process  | Secrets must enter Wrangler without command-line or log disclosure.                            |
| Production D1 to recovery directory | Plaintext exports contain sensitive rows and must never enter the worktree or broad ACL scope. |
| Non-idempotent SQL to live shards   | Duplicate/out-of-order DDL or concurrent writes can make preservation ambiguous.               |
| Live smoke to financial tables      | Test checkout must be durably journaled and conclusively removed.                              |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                          |
| --------- | ---------------------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-H7W-01  | Spoofing               | Cloudflare/D1 target            | mitigate    | Wrapper allowlists identity, config, and three exact databases; every invocation is RTK-prefixed.                        |
| T-H7W-02  | Tampering              | Migration order/schema          | mitigate    | Missing-only 0015→0016, per-shard reclassification, exact schema/index/FK and canonical hash gates.                      |
| T-H7W-03  | Repudiation            | Export/DDL/cleanup              | mitigate    | Absolute paths, hashes, timestamps, migration disposition, durable UAT journal, DELETE and zero-row evidence.            |
| T-H7W-04  | Information Disclosure | `.env`, backups, query evidence | mitigate    | Child-only env, output secret scan, restrictive ACL, no content logs, outside-worktree storage, disposable raw evidence. |
| T-H7W-05  | Denial of Service      | Production shards               | mitigate    | All backups proven first, quiet-window gate, one shard at a time, immediate first-failure stop.                          |
| T-H7W-06  | Elevation of Privilege | Branch/direct writes            | mitigate    | Authenticated 403 and two 409 guard assertions.                                                                          |

</threat_model>

<verification>
1. All remote Wrangler calls traverse the proven RTK-invoked secret-safe wrapper.
2. Three outside-worktree recovery exports pass ACL, hash, and local-import proof before DDL.
3. Per-shard immediate export/baseline and concurrency classification avoid false snapshot claims.
4. Missing-only 0015/0016, exact schema/index/FK, canonical all-field 0016 hash, and integrity checks pass.
5. Full 200/401/403/409 and two-client realtime smoke passes.
6. Every created transaction has durable ID journal, successful DELETE, and authenticated zero transaction/ledger rows.
7. Browser result or pre-transaction launch limitation and exact service-worker hash are recorded.
8. No source/config edit, commit, build, deployment, or `.gitignore` change occurs.
</verification>

<success_criteria>

- Recoverable, verified backups exist outside the repository for all three shards before first DDL.
- All shards reach required 0015/0016 schema through missing-only ordered execution.
- 0016 proves every copied `pengaturan` field through canonical expected-post digest and exact metadata checks.
- Concurrent production activity is quiescent or explicitly attributed/inconclusive; never presented as atomic proof.
- Production login, branch isolation, direct-write guards, checkout, realtime, cleanup, and residue queries pass.
- Production `sw.js` hash remains exact.
- Only `260718-h7w-SUMMARY.md` is added inside the repository; no changes are committed.
  </success_criteria>

<output>
Create `.planning/quick/260718-h7w-backup-and-apply-missing-d1-production-m/260718-h7w-SUMMARY.md` when done. Retain backups outside the repo. Do not commit anything.
</output>
