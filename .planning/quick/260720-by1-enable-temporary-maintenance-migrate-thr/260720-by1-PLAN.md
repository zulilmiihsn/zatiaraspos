---
phase: quick-260720-by1-enable-temporary-maintenance-migrate-thr
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/maintenance/index.html
  - .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/maintenance/_worker.js
  - .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/wrangler-safe-wrapper.mjs
  - .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/uat-fail-closed.mjs
  - .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/recovery-manifest.json
  - .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/260720-by1-SUMMARY.md
autonomous: true
requirements:
  - QUICK-MAINTENANCE-D1-MIGRATION
must_haves:
  truths:
    - 'After all gates finish, one final sealed production build is created and hash-manifested before maintenance starts; no later rebuild is allowed.'
    - 'A minimal no-store 503 maintenance deployment replaces the canonical production alias before D1 mutation, while immutable historical deployment URLs are explicitly acknowledged as still reachable.'
    - 'Each production D1 shard is confirmed as version production and has an exact Cloudflare Time Travel bookmark and timestamp before DDL.'
    - 'Only missing 0015 then 0016 migrations execute, one shard at a time, with first-failure stop and exact schema/data preservation proof.'
    - 'Any migration failure leaves maintenance active; no automatic Time Travel restore or app redeploy occurs on inconsistent schema.'
    - 'After all shards pass, the exact pre-maintenance app artifact is redeployed without rebuilding and matches its recorded hashes.'
    - 'Live smoke journals intent before checkout, resolves the committed transaction, and proves successful cleanup with zero transaction, ledger, and idempotency residue.'
  artifacts:
    - path: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/maintenance/_worker.js'
      provides: 'Pages maintenance Worker returning explicit no-store HTTP 503 for UI and API paths'
    - path: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/wrangler-safe-wrapper.mjs'
      provides: 'Secret-safe allowlisted boundary for Pages, D1 metadata/bookmarks, read-only audit, and exact migration files'
    - path: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/uat-fail-closed.mjs'
      provides: 'Durably journaled authenticated checkout/realtime/browser smoke and fail-closed cleanup'
    - path: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/recovery-manifest.json'
      provides: 'ACL-restricted, fsynced Time Travel identities/bookmarks/timestamps/deadlines/manual restore commands'
    - path: '.svelte-kit/cloudflare'
      provides: 'Final sealed production application artifact restored after migration without a later rebuild'
    - path: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/260720-by1-SUMMARY.md'
      provides: 'Sanitized maintenance, bookmark, migration, artifact, and live-smoke evidence'
  key_links:
    - from: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/wrangler-safe-wrapper.mjs'
      to: 'wrangler.pages.jsonc'
      via: 'allowlisted project, three production database identities, and explicit config'
      pattern: 'zatiaras-(samarinda|balikpapan|berau)-group'
    - from: 'drizzle/meta/_journal.json'
      to: 'drizzle/0015_session_page_unlocks.sql and drizzle/0016_pin_hash.sql'
      via: 'pre-recorded SHA-256 and missing-only 0015→0016 execution'
      pattern: '001(5_session_page_unlocks|6_pin_hash)'
    - from: '.svelte-kit/cloudflare'
      to: 'zatiaraspos Pages production'
      via: 'recursive pre-maintenance manifest verification followed by Direct Upload without rebuild'
      pattern: 'sw.js|_worker.js'
    - from: '.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/uat-fail-closed.mjs'
      to: '/api/pos/transaction and /api/transaksi-kasir'
      via: 'pre-POST idempotency journal, transaction resolution, DELETE, and zero-residue queries'
      pattern: 'idempotency_key|transaction_id'
---

<objective>
Put production into an explicit temporary 503 maintenance state, establish Time Travel recovery bookmarks, apply only missing D1 migrations 0015/0016 to all three shards, restore the exact built app, and prove production through fail-closed live smoke.

Purpose: Block canonical-alias writes while whole-file atomic D1 execution prevents statement interleaving and audit classification bounds writes from still-reachable immutable deployment hosts, retaining a Cloudflare-native recovery point and exact artifact provenance.
Output: Uncommitted maintenance/wrapper/harness/summary artifacts under this task directory; no application source edit, commit, staging, plaintext database export, or automatic restore.
</objective>

<execution_context>
@C:/Users/ASUS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ASUS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260718-esb-deploy-production-and-run-live-smoke-tes/260718-esb-PLAN.md
@.planning/quick/260718-esb-deploy-production-and-run-live-smoke-tes/260718-esb-SUMMARY.md
@.planning/quick/260718-h7w-backup-and-apply-missing-d1-production-m/260718-h7w-PLAN.md
@wrangler.pages.jsonc
@drizzle/meta/_journal.json
@drizzle/0015_session_page_unlocks.sql
@drizzle/0016_pin_hash.sql
@package.json
@scripts/uat-live-realtime.mjs
@scripts/uat-live-ui-two-device.mjs
</context>

<query_contract>
The wrapper must map query IDs to these canonical SQL templates internally and reject caller-supplied SQL. Whitespace normalization is allowed only inside the wrapper's own constants; callers pass a query ID plus validated typed parameters.

- `Q_AUTH_SCHEMA`: `PRAGMA table_info("auth_sessions");`
- `Q_SETTINGS_SCHEMA`: `PRAGMA table_info("pengaturan");`
- `Q_SETTINGS_INDEX_LIST`: `PRAGMA index_list("pengaturan");`
- `Q_SETTINGS_INDEX_INFO`: `PRAGMA index_info("idx_pengaturan_branch");`
- `Q_SETTINGS_FKS`: `PRAGMA foreign_key_list("pengaturan");`
- `Q_SETTINGS_TEMP_TABLE`: `SELECT COUNT(*) AS table_count FROM sqlite_master WHERE type = 'table' AND name = '__new_pengaturan';`
- `Q_QUICK_CHECK`: `PRAGMA quick_check;`
- `Q_INTEGRITY_CHECK`: `PRAGMA integrity_check;`
- `Q_AUTH_UNLOCK_INVALID`: `SELECT COUNT(*) AS invalid_count FROM auth_sessions WHERE unlocked_pages IS NULL OR unlock_expires_at IS NULL;`
- `Q_CORE_COUNTS`: `SELECT 'profil' AS table_name, COUNT(*) AS row_count, NULL AS amount_sum FROM profil UNION ALL SELECT 'auth_sessions', COUNT(*), NULL FROM auth_sessions UNION ALL SELECT 'pengaturan', COUNT(*), NULL FROM pengaturan UNION ALL SELECT 'produk', COUNT(*), NULL FROM produk UNION ALL SELECT 'sesi_toko', COUNT(*), NULL FROM sesi_toko UNION ALL SELECT 'buku_kas', COUNT(*), COALESCE(SUM(nominal), 0) FROM buku_kas UNION ALL SELECT 'transaksi_kasir', COUNT(*), COALESCE(SUM(nominal), 0) FROM transaksi_kasir;`
- `Q_SETTINGS_PRE_0016`: `SELECT id, cabang_id, pin, NULL AS pin_hash, halaman_terkunci, nama_toko, alamat, telepon, instagram, ucapan, created_at, updated_at FROM pengaturan ORDER BY cabang_id, id;`
- `Q_SETTINGS_POST_0016`: `SELECT id, cabang_id, pin, pin_hash, halaman_terkunci, nama_toko, alamat, telepon, instagram, ucapan, created_at, updated_at FROM pengaturan ORDER BY cabang_id, id;`
- `Q_FINANCIAL_IDENTITIES`: `SELECT 'buku_kas' AS source, id, transaction_id, idempotency_key, nominal, created_at FROM buku_kas UNION ALL SELECT 'transaksi_kasir', id, transaction_id, NULL AS idempotency_key, nominal, created_at FROM transaksi_kasir ORDER BY source, id;`
- `Q_AUDIT_WINDOW`: `SELECT id, cabang_id, action, entity_type, entity_id, transaction_id, amount, created_at FROM audit_logs WHERE created_at >= :start_utc AND created_at <= :end_utc AND entity_type IN ('buku_kas', 'transaksi_kasir', 'pengaturan') ORDER BY created_at, id;` The wrapper must validate RFC3339 parameters and safely substitute quoted literals itself.
- `Q_IDEMPOTENCY_RESIDUE`: `SELECT COUNT(*) AS residue_count FROM buku_kas WHERE cabang_id = :branch AND idempotency_key = :idempotency_key;` The wrapper must allowlist the branch and validate the UAT key format before safe literal substitution.

`Q_FINANCIAL_IDENTITIES` and `Q_AUDIT_WINDOW` may expose transaction identifiers, idempotency keys, amounts, or audit identifiers. Their stdout must never reach the terminal, orchestration transcript, planning artifacts, or summary. The wrapper must stream those two query results directly into restrictive current-process-only OS-temporary files, compare them internally, emit only sanitized counts/delta verdicts, and securely delete the raw files in `finally`. Any redirection, ACL, comparison, or deletion failure is a hard stop.

No other remote SQL is permitted. In particular, mutating `--command`, caller-provided SQL, and split migration statements are rejected.
</query_contract>

<tasks>

<task type="auto">
  <name>Task 1: Seal the app artifact, create safety tooling, and activate maintenance</name>
  <files>.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/maintenance/index.html, .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/maintenance/_worker.js, .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/wrangler-safe-wrapper.mjs, .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/uat-fail-closed.mjs</files>
  <action>
Capture UTC start, branch/HEAD, `rtk git status --short --branch`, and `rtk git diff --check`. Do not switch, stash, reset, stage, or commit. Using `apply_patch` only, create four uncommitted task-local artifacts: maintenance `index.html`, maintenance `_worker.js`, secret-safe Wrangler wrapper, and fail-closed UAT harness. Do not edit application source/config or `.gitignore`.

The maintenance Worker must intercept every method/path. For `/api/*` return JSON HTTP 503; for all other paths return a concrete maintenance HTML HTTP 503. Both responses require a unique task marker, `Cache-Control: no-store, no-cache, must-revalidate`, `Pragma: no-cache`, and `Retry-After`; do not forward to `env.ASSETS` or application code. Keep copy factual and emoji-free. Syntax-check Worker/harness/wrapper with RTK-prefixed commands and run task-local allowlist/redaction self-tests before remote access.

The wrapper must parse `.env` into child environment without printing names/values, invoke `rtk pnpm exec wrangler` with argv and `shell: false`, scan/redact outputs against loaded secret values, and reject everything not allowlisted. Allow only: expected-account identity; production Pages secret-name listing; Pages production deployment listing; Pages Direct Upload of exactly the task maintenance directory or the exact `.svelte-kit/cloudflare` directory; D1 `info`; D1 `time-travel info`; exact query IDs/canonical read-only SQL in `<query_contract>`; and remote `d1 execute --file` of only the absolute checked-in 0015/0016 files when their current SHA-256 equals the wrapper's pre-recorded manifest. Require exact project `zatiaraspos`, branch `main`, and the three config database names/IDs. For every D1 call, require `--config wrangler.pages.jsonc`; for Pages commands, first parse/validate that config and project locally but do not pass unsupported `--config` to `wrangler pages`. A migration is exactly one child process/API operation with the whole checked-in file; reject `--command` mutation, split statements, multiple files, unknown SQL/files/targets/directories, raw `wrangler`, and raw `npx`. Invoke every Cloudflare command through `rtk node <task-wrapper> ...`.

Before downtime, verify Cloudflare identity and encrypted production secret names `POS_PRICE_SIGNING_KEY` and `POS_PRICE_SIGNING_KEY_ID`. Capture the current canonical production deployment ID plus immutable URL and realtime Worker `/health`. Record SHA-256 of both migration files. Run release gates first: structurally valid `rtk pnpm deploy:check` without persisting a signing key, `rtk pnpm check`, `rtk pnpm lint`, and `rtk pnpm test:all`. These gates may internally invoke builds; do not count or describe them as the sealed production build. After every gate passes, run exactly one final sealed `rtk pnpm build`. Stop before maintenance on any failure and never build again after this final sealed build.

After the single build, create a stable sorted recursive SHA-256 manifest for every file under `.svelte-kit/cloudflare`, record its manifest hash plus individual `_worker.js` and `sw.js` hashes, and mark this directory read-only for the run where practical. Never rebuild after this point. Deploy the maintenance directory through the wrapper with Pages Direct Upload to project `zatiaraspos`, branch `main`, and `--commit-dirty=true`. Capture maintenance deployment ID and immutable URL.

Poll the maintenance deployment URL and canonical alias `https://zatiaraspos.pages.dev` with cache-busting until `/`, `/login`, `/api/session`, `/api/veriflogin`, `/api/pos/transaction`, `/api/buku-kas`, and `/api/transaksi-kasir` all return the exact task maintenance marker and HTTP 503 for representative GET/POST requests. Require no app-shaped 200/401/403/409/500 response on the canonical alias. Explicitly record that prior immutable Pages deployment URLs remain reachable and may still receive traffic; maintenance quiesces the canonical production alias, not every historical hostname or already-open client. Only this confirmed canonical maintenance gate authorizes Task 2. If deployment/verification fails, stop with no D1 command beyond read-only metadata.
</action>
<verify>
<automated>Task-local syntax/self-tests pass; all quality gates plus one final sealed build pass; secrets are listed by name only; current deployment/Worker health and complete artifact manifest are recorded; canonical production UI/API probes all return marked no-store 503 while historical immutable-host reachability is documented.</automated>
</verify>
<done>The canonical production alias is explicitly quiesced behind verified maintenance, immutable-host limitation is recorded, and a sealed-by-manifest app artifact plus allowlisted wrapper/harness are ready without source edits.</done>
</task>

<task type="auto">
  <name>Task 2: Bookmark Time Travel and migrate all production shards under maintenance</name>
  <files>.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/recovery-manifest.json, .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/260720-by1-SUMMARY.md</files>
  <action>
Keep maintenance active throughout this task and re-probe its marker before each shard. Process `zatiaras-samarinda-group`, `zatiaras-balikpapan-group`, then `zatiaras-berau-group` sequentially. Use only the wrapper.

Before any audit or DDL, call `d1 info <database> --json` and require the exact configured database identity plus `version: production`; non-production state is a hard stop. Capture a fresh UTC RFC3339 timestamp and call `d1 time-travel info <database> --timestamp <timestamp> --json`. Cloudflare D1 Time Travel is always-on for production; require a non-empty exact bookmark associated with that database/timestamp. Use a conservative recovery deadline of exactly bookmark timestamp plus 7 days because Free retention is at least 7 days even though Paid may retain 30 days. Escalate for an explicit restore/accept decision before that deadline if the run blocks.

Before the first DDL, atomically persist all three recovery entries to task-local `recovery-manifest.json` using write-temp, file fsync, atomic rename, parent-directory fsync where supported, restrictive current-user/SYSTEM ACL, readback, JSON-schema validation, and equality verification. Each entry must contain database name, database ID, confirmed `version: production`, UTC timestamp, exact bookmark, conservative 7-day deadline, and an exact non-executed restore command routed through the task wrapper: `rtk node <task-wrapper> time-travel-restore --database <database-name> --bookmark <bookmark> --confirm-bookmark <bookmark>`. The wrapper's `time-travel-restore` mode must use the proven child-process environment loader, revalidate database name/ID and bookmark format, require an interactive destructive confirmation plus explicit `ALLOW_D1_TIME_TRAVEL_RESTORE=YES`, and remain forbidden during this task. It must never route restore through direct `rtk pnpm exec wrangler`. The manifest contains no secret. Do not run restore and do not create plaintext exports. If all three entries are not durably verified, no DDL may start.

After all three production identities and pre-DDL bookmarks are secured, audit each shard immediately before its possible DDL. Use allowlisted read-only queries for exact `PRAGMA table_info`, `index_list/index_info`, `foreign_key_list`, temporary-table presence, integrity, and sanitized counts/financial aggregates. Classify 0015 applied only when both unlock columns with expected definitions exist, missing only when both are absent, partial otherwise. Classify 0016 applied only when exact `pin_hash` schema/index/FK state exists and `__new_pengaturan` is absent, missing only when the pre-0016 table is intact, partial otherwise. Partial/ambiguous state stops all further work with maintenance remaining active.

For each shard, capture pre-DDL invariants through the exact query contract: core counts, full financial identities/idempotency keys/amounts, audit-window start, session schema, and settings schema/data. Canonical maintenance blocks the main hostname but historical immutable deployments remain reachable, so do not call the database globally quiescent and do not require volatile counts to remain blindly unchanged. For 0016, generate a cryptographically random ephemeral HMAC key in a restrictive OS-temp location, never task files. Query every copied `pengaturan` field into process-private raw evidence, canonicalize ordered rows with stable typed length-prefix encoding, and compute keyed HMACs for actual-pre and expected-post projections. Expected-post changes only `pin='1234'` to null and introduces null `pin_hash`; every other copied field must remain byte-identical. Never print/store the HMAC key, raw PIN/evidence, or HMAC digests in the summary.

Build the missing list from only 0015 then 0016. Execute only an exact missing file through the wrapper after it recomputes and matches the recorded SHA-256. Each file must be submitted as one whole remote D1 file operation/API call; the wrapper rejects statement splitting or partial execution. Cloudflare D1 batch statements execute sequentially/non-concurrently and roll back the whole sequence on failure, and failed remote file execution returns the database to its original state. This closes interleaving inside each exact migration batch even though old immutable hosts may write before or after it. Never use `d1 migrations apply`, arbitrary SQL mutation, or a present migration.

After 0015 require exact column names/types/not-null/defaults and zero invalid unlock rows; existing/new session counts may legitimately change outside the atomic batch, so classify deltas rather than falsely require equality. After 0016 require exact ordered columns/types/defaults/PK, exact `idx_pengaturan_branch`, expected empty FK list, no temporary table, zero raw default `1234`, and internal expected-post HMAC equality across every field. If the immediate settings HMAC differs, inspect the exact audit window. A proven post-batch settings write is a concurrency blocker/inconclusive preservation result, not automatic migration corruption; an unexplained mismatch is preservation failure. Either case stops later shards with maintenance active. Run `PRAGMA quick_check`; if unavailable use `PRAGMA integrity_check`, and if both remote pragmas are unsupported use a read-only schema/full-scan fallback plus the Time Travel bookmark as the recovery boundary, clearly recording the reduced integrity signal.

After each shard compare financial identity sets, transaction IDs, idempotency keys, amounts, counts/sums, and the exact audit window. Every pre-batch financial row must remain unless an explicit same-window audited void accounts for it; every new append must have a new identity/idempotency key and matching audit evidence. Legitimate pre/post old-host traffic is recorded as traffic, not blamed on schema DDL. Unexplained deletion, mutation, duplicate key, or amount drift fails. For migration-touched `pengaturan`, require full HMAC preservation unless a post-batch audited write makes the gate explicitly inconclusive; do not weaken it to a count-only check. Destroy the HMAC key and raw evidence immediately after internal comparison; record only PASS/FAIL/inconclusive reason. Stop on the first command, hash, schema, HMAC, integrity, or data-invariant failure. On any failure after maintenance begins: keep canonical maintenance active, preserve the durable manifest, do not auto-restore, do not retry DDL, do not migrate later shards, and do not redeploy the app onto inconsistent schema. Write a blocker summary with exact shard/migration/bookmark/deadline/manual restore command and escalate before the 7-day deadline.
</action>
<verify>
<automated>All three production identities/bookmarks are fsynced and readback-validated before DDL; each exact missing migration runs as one whole atomic remote file batch in order; schema/integrity/HMAC gates pass; financial identity deltas are unchanged or fully audit-attributed without false volatile-count claims.</automated>
</verify>
<done>All three production shards are consistently migrated under canonical maintenance with durable 7-day-conservative recovery instructions, atomic whole-file DDL, honest old-host concurrency treatment, and no disclosed PIN/evidence/key/digest.</done>
</task>

<task type="auto">
  <name>Task 3: Restore the sealed app artifact and run fail-closed production smoke</name>
  <files>.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/uat-fail-closed.mjs, .planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/260720-by1-SUMMARY.md</files>
  <action>
Run only after Task 2 passes on all three shards. Recompute the complete `.svelte-kit/cloudflare` recursive manifest and require exact equality with Task 1, including `_worker.js` and `sw.js`; do not rebuild. Redeploy that exact directory through the wrapper as Pages Direct Upload to project `zatiaraspos`, branch `main`, `--commit-dirty=true`. Capture the new immutable app deployment ID/URL. Poll immutable URL and production alias until maintenance marker disappears, `/` and `/login` return app HTTP 200, protected APIs return app HTTP 401 unauthenticated, realtime Worker `/health` returns its exact healthy JSON, and production `/sw.js` equals the Task 1 hash. If restoration deploy or verification fails, stop and report; do not mutate D1 further.

Use the task-local fail-closed harness, not the existing mutating UAT scripts. It must load `UAT_PASSWORD` without logging it and verify public 200, unauthenticated 401, cashier/owner login 200, authenticated cross-branch 403, and both direct-write 409 guards. Open two realtime clients and prepare one exact quote/checkout request.

Before sending checkout POST, atomically journal `{ branch, idempotency_key, status: 'pending' }` using write-temp/fsync/rename. After a successful response, atomically add `transaction_id` before awaiting realtime or other assertions. On missing/lost response, retry the identical idempotent checkout request to recover `transaction_id`; if still unavailable, use the wrapper's single allowlisted read-only lookup of `buku_kas.transaction_id` by validated branch/idempotency key. No second checkout with a different key may start while any journal is pending.

Cleanup runs in an unsuppressed `finally`. Require successful owner DELETE by exact transaction ID, then authenticated API queries prove zero `transaksi_kasir` and zero `buku_kas` rows by transaction ID, and the wrapper's allowlisted D1 read-only query proves zero ledger row by idempotency key. Mark journal `cleaned` only after all three zero-residue checks. On restart, cleanup-only mode handles any non-cleaned journal before new work; unresolved cleanup is a release blocker.

Run the harness's two-device browser mode when Chrome/CDP is available with the same pre-POST journal and cleanup rules. A browser launch failure before checkout may be recorded as an exact limitation; any journaled checkout requires cleanup/zero residue before classification. Do not claim browser success otherwise.

Write `260720-by1-SUMMARY.md` with pre/maintenance/restored deployment IDs and URLs, explicit canonical-alias-only maintenance limitation, secret-name proof, final-sealed-build manifest hashes, Worker health, three D1 versions/bookmarks/timestamps, conservative 7-day deadlines and escalation requirement, per-shard atomic whole-file migration disposition, schema/integrity/financial identity/HMAC PASS/FAIL/inconclusive status only, 200/401/403/409 matrix, two-client realtime, durable journal, DELETE and zero-residue proof, browser result/limitation, sw hash, downtime interval, and final verdict. Never include credentials, PINs, raw evidence, HMAC key/digest, cookies, tokens, or response headers. Rerun `rtk git diff --check` and status; leave only task-local planning artifacts uncommitted. Never stage/commit.
</action>
<verify>
<automated>The exact pre-maintenance artifact manifest is redeployed without rebuild; immutable/prod app URLs, Worker health, and sw hash pass; harness proves 200/401/403/409, two realtime clients, successful DELETE, zero transaction/ledger/idempotency residue, and a cleaned journal; final diff check passes.</automated>
</verify>
<done>Maintenance is removed only after consistent migration, exact app artifact is restored, live production behavior passes with fail-closed cleanup, and no source edit/stage/commit occurred.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                            |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Dirty worktree/build to Pages Direct Upload | One sealed artifact must survive a temporary maintenance replacement without rebuild drift.            |
| `.env` to Wrangler child                    | Cloudflare credentials must remain child-only and absent from logs/argv.                               |
| Pages maintenance to production clients     | Canonical alias traffic is blocked, but historical immutable deployment URLs remain reachable.         |
| Checked-in migrations to three D1 shards    | Non-idempotent DDL requires production identity, bookmark, missing-only order, and preservation gates. |
| Live checkout to financial tables           | Lost responses/process failures must still resolve and remove the exact idempotent transaction.        |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                                                                                    |
| --------- | ---------------------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| T-BY1-01  | Spoofing               | Cloudflare project/D1 target     | mitigate    | Wrapper allowlists project, branch, config, three identities, production version, and permitted operations.                        |
| T-BY1-02  | Tampering              | App/migration artifacts          | mitigate    | Full app manifest plus migration SHA-256; each migration is one whole atomic D1 file batch and split execution is rejected.        |
| T-BY1-03  | Repudiation            | Maintenance/bookmark/DDL/cleanup | mitigate    | Record deployment IDs, downtime, exact bookmarks/timestamps, dispositions, and durable cleanup journal.                            |
| T-BY1-04  | Information Disclosure | Secrets/PIN/HMAC/session data    | mitigate    | Child-only env, redaction tests, keyed internal comparison, destroyed raw evidence/key, sanitized summary.                         |
| T-BY1-05  | Denial of Service      | Maintenance and shard migration  | mitigate    | Explicit authorized canonical 503 window, durable bookmarks/deadlines first, sequential migration, first-failure maintenance hold. |
| T-BY1-06  | Elevation of Privilege | Branch/direct-write APIs         | mitigate    | Authenticated 403 and direct-write 409/409 assertions after restore.                                                               |

</threat_model>

<verification>
1. Gates pass, then one final sealed app build is recorded before downtime; no build occurs afterward.
2. Maintenance Direct Upload returns marked no-store 503 on the canonical alias while immutable-host reachability is acknowledged.
3. All databases report production version and all bookmark/timestamp/7-day-deadline/manual commands are fsynced/readback before DDL.
4. Missing-only 0015→0016 each runs as one whole atomic remote file operation and passes exact schema/index/FK/integrity, keyed touched-table preservation, and identity/audit-aware financial invariants.
5. Any failure path keeps maintenance active and forbids restore/app redeploy automatically.
6. Exact recorded app manifest is restored without rebuild; immutable/prod URLs, health, and sw hash pass.
7. Fail-closed smoke proves 200/401/403/409, quote/checkout, two realtime clients, DELETE, and zero transaction/ledger/idempotency residue.
8. Browser result is proven or precisely limited, and no source edit/stage/commit occurs.
</verification>

<success_criteria>

- Production downtime begins only after the final sealed artifact is ready; earlier gates may have built internally, but no rebuild follows sealing.
- Every production shard has a valid Time Travel recovery bookmark and required 0015/0016 schema.
- Recovery manifest records a conservative 7-day deadline and exact manual restore command for each shard; blocked runs escalate before expiry.
- Full pengaturan and financial preservation checks pass without disclosing sensitive values.
- Canonical maintenance and atomic D1 batch behavior prevent interleaving inside DDL; immutable-host pre/post writes are separately audit-classified.
- Maintenance remains active on any incomplete or inconsistent migration.
- The exact original app artifact replaces maintenance after all shards pass.
- Complete authenticated/realtime smoke leaves no UAT financial residue.
- Only task-local wrapper, harness, maintenance, plan, and summary artifacts remain uncommitted.
  </success_criteria>

<output>
Create `.planning/quick/260720-by1-enable-temporary-maintenance-migrate-thr/260720-by1-SUMMARY.md` when done. Do not stage or commit any file.
</output>
