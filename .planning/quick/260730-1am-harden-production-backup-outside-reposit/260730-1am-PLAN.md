---
phase: quick-260730-1am-harden-production-backup-outside-reposit
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - scripts/d1-backup.mjs
  - scripts/d1-backup.test.mjs
  - scripts/uat-live-realtime.mjs
  - OWNER-GUIDE.md
  - OPERATIONS-RUNBOOK.md
  - HANDOVER-CHECKLIST.md
  - KNOWN-LIMITATIONS.md
autonomous: true
requirements:
  - QUICK-PRODUCTION-BACKUP-HANDOVER
must_haves:
  truths:
    - 'Production D1 backup requires an explicit absolute destination outside the repository/workspace and can never write raw SQL into Git.'
    - 'A successful backup contains exactly one nonempty identity-verified SQL export for each of the three configured production shards plus a verified SHA-256 manifest.'
    - 'Backup children run through RTK without a shell, perform only info/export operations, and never disclose loaded environment secrets.'
    - 'Owner and operator documents describe actual roles, QRIS confirmation, offline limits, branch-scoped reports, backup verification, destructive restore boundaries, and access handover.'
    - 'Automated technical readiness includes separate quality gates, staged secret scan before full diff output, fresh external backup, read-only live smoke, and authenticated live UAT only when a store session is already active.'
    - 'Live UAT cannot succeed unless realtime events match the exact created transaction, ambiguous responses recover through identity-verified allowlisted read-only D1 lookup, and cleanup proves zero transaction, ledger, and idempotency residue.'
    - 'One exact source/docs candidate commit is pushed to dev; later GSD metadata is separate, no tag is created, and eventual v2.0.2 must target the recorded source/docs SHA only after explicit owner approval.'
    - 'No Cloudflare deployment, store-session mutation, restore, unrelated staging, force operation, or owner-acceptance claim occurs.'
  artifacts:
    - path: 'scripts/d1-backup.mjs'
      provides: 'Fail-closed three-shard production export and SHA-256 manifest verification'
    - path: 'scripts/d1-backup.test.mjs'
      provides: 'Network-free backup safety regression tests'
    - path: 'scripts/uat-live-realtime.mjs'
      provides: 'Journaled authenticated UAT, exact realtime correlation, D1 ambiguity recovery, and zero-residue cleanup'
    - path: 'OWNER-GUIDE.md'
      provides: 'Owner-facing application guide'
    - path: 'OPERATIONS-RUNBOOK.md'
      provides: 'Backup, incident, offline queue, and recovery-escalation runbook'
    - path: 'HANDOVER-CHECKLIST.md'
      provides: 'Access, UAT, credential-rotation, release, and acceptance checklist'
    - path: 'KNOWN-LIMITATIONS.md'
      provides: 'Current product and operations limitations'
    - path: '<external-absolute-backup-dir>/<run-id>/manifest.sha256.json'
      provides: 'Fresh verified three-shard production backup manifest'
  key_links:
    - from: 'scripts/d1-backup.mjs'
      to: 'wrangler.pages.jsonc'
      via: 'exact binding/name/database_id set validation'
      pattern: 'DB_(SAMARINDA|BALIKPAPAN|BERAU)_GROUP'
    - from: 'scripts/d1-backup.mjs'
      to: 'rtk pnpm exec wrangler d1 info/export'
      via: 'shell:false allowlisted child argv'
      pattern: 'd1.*(info|export)'
    - from: 'scripts/uat-live-realtime.mjs'
      to: '/api/sesi-toko'
      via: 'authenticated read-only active-session preflight before journal or checkout'
      pattern: 'is_active=true'
    - from: 'scripts/uat-live-realtime.mjs'
      to: 'production D1 buku_kas'
      via: 'identity-verified fixed read-only lookup by validated idempotency key'
      pattern: 'idempotency_key'
    - from: 'scripts/uat-live-realtime.mjs'
      to: 'realtime BranchEventPayload'
      via: 'exact returned transaction_id or buku_kas id correlation'
      pattern: 'transaction_id|buku_kas_id'
    - from: 'OPERATIONS-RUNBOOK.md'
      to: 'scripts/d1-backup.mjs'
      via: 'RTK-prefixed backup and manifest verification commands'
      pattern: 'rtk pnpm d1:backup'
---

<objective>
Harden production backup, create the owner handover package, and publish one verified operations-only technical candidate for later owner acceptance.

Purpose: Prevent production SQL or secrets entering Git, prove a complete recoverable three-shard snapshot, leave truthful operating instructions, and separate automated technical readiness from human acceptance.
Output: Tested backup/UAT tooling, four root handover documents, fresh external production backup, sanitized proof, and one exact atomic source/docs candidate commit pushed to `dev`. Automated work creates no tag and performs no Cloudflare deployment.
</objective>

<execution_context>
@D:/Projects/zatiaraspos/.claude/get-shit-done/workflows/execute-plan.md
@D:/Projects/zatiaraspos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@package.json
@.gitignore
@wrangler.pages.jsonc
@wrangler.realtime.jsonc
@scripts/d1-backup.mjs
@scripts/d1-restore.mjs
@scripts/uat-live-realtime.mjs
@scripts/uat-live-ui-two-device.mjs
@src/lib/server/realtimePublisher.ts
@src/routes/api/sesi-toko/+server.ts
@OFFLINE_POS.md
@PLAN-11of11-RELEASE-READINESS-CLOSURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden operations, prove technical readiness, and push exact candidate commit</name>
  <files>.gitignore, package.json, scripts/d1-backup.mjs, scripts/d1-backup.test.mjs, scripts/uat-live-realtime.mjs, OWNER-GUIDE.md, OPERATIONS-RUNBOOK.md, HANDOVER-CHECKLIST.md, KNOWN-LIMITATIONS.md</files>
  <behavior>
    - Missing, relative, repo-contained, workspace-contained, or symlink/junction-resolved-inside-repo backup destinations fail before directory creation or any remote child call.
    - Config with fewer/more than three entries, unexpected/missing bindings, duplicate names/IDs, absent IDs, or non-production identity fails before export.
    - Each valid shard runs one identity query then one export; backup mode cannot reach D1 execute, migrations, restore, delete, or arbitrary command.
    - Missing/zero-byte/non-SQL output, shard-set mismatch, or hash/readback mismatch produces no COMPLETE marker.
    - Successful manifest contains exactly three unique relative SQL files with matching byte counts and SHA-256 hashes.
    - Backup/UAT child argv begins with RTK, uses `shell:false`, and sanitized diagnostics contain none of the loaded secret values.
    - Live UAT performs no mutation when active-session preflight returns zero/multiple sessions; execution pauses as a blocking `human_needed` gate before staging, commit, or push.
    - Journal exists durably before checkout; ambiguous checkout recovery uses only the configured shard's identity-verified fixed read-only D1 query by validated idempotency key.
    - Cleanup-only works when a retained journal has an idempotency key but no transaction ID.
    - DELETE 404 is accepted only after transaction ID, ledger identity, and idempotency key all prove zero residue; any nonzero/unknown result retains the journal and fails.
    - Both realtime clients must receive an insert event whose `transaction_id` equals the returned transaction ID or whose `id` equals the returned buku_kas ID; unrelated branch inserts never satisfy UAT.
    - UAT loads only UAT_PASSWORD, CLOUDFLARE_API_TOKEN, and CLOUDFLARE_ACCOUNT_ID into minimal child environments, redacts all three values, and fails before checkout when either Cloudflare credential is missing.
    - Tests inject fake runners/filesystems/fetch/socket responses and make no Cloudflare request or remote mutation.
  </behavior>
  <action>
Perform these stages in order. Stage A is mandatory before editing any planned file.

**A. Preflight and immutable baseline before edits**

Run `rtk git status --short --branch`, `rtk git diff --check`, `rtk git diff --cached --name-only`, `rtk git branch --show-current`, `rtk git rev-parse HEAD`, `rtk git fetch origin dev --tags`, `rtk git rev-parse origin/dev`, `rtk git tag --list v2.0.2`, and `rtk git ls-remote --heads --tags origin`. Require branch `dev`, `HEAD == origin/dev`, an empty index, and no existing tracked/untracked change at any of the nine planned file paths. Existing untracked root `PLAN-9of9-BUG-DEBT-CLOSURE.md`, `PLAN-10of10-POS-DATA-INTEGRITY.md`, `PLAN-11of11-RELEASE-READINESS-CLOSURE.md`, this quick-plan directory, and unrelated `.planning/quick` work are allowed but must be recorded and preserved. Require local and remote `v2.0.2` to be absent for future release eligibility, but do not reserve or create it. Stop before edits on overlap, divergence, staged content, or tag conflict. Never stash, reset, checkout, clean, delete, or modify unrelated work.

**B. Harden external production backup**

Refactor `scripts/d1-backup.mjs` into import-safe helpers plus guarded CLI. Require `--output-dir <absolute-directory>` and optionally accept `--env-file <path>`. Resolve the repo root and canonical destination. For a missing destination, canonicalize its nearest existing ancestor and suffix; after creation re-run `realpath`. Reject equality with or ancestry under the repo/workspace, including symlink/junction redirects. Never default to current directory, repo `backups/`, OS temp, or home. Create a unique UTC/UUID run directory and restrictive permissions where supported.

Parse `wrangler.pages.jsonc`; require exact set equality with `DB_SAMARINDA_GROUP`, `DB_BALIKPAPAN_GROUP`, and `DB_BERAU_GROUP`, exactly three unique names/IDs, and no fallback binding. If `--env-file` is supplied, load only `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` into child memory, reject duplicate/malformed entries, and never log values.

All children must be `spawn`/`spawnSync` of `rtk` with argv beginning `pnpm exec wrangler`, `shell:false`, piped bounded output, and value-based redaction. Backup allowlist is only:

1. `d1 info <name> --config wrangler.pages.jsonc --json`
2. `d1 export <name> --config wrangler.pages.jsonc --remote --skip-confirmation --output <external-file>`

Remove `recordRun` and every backup-time `d1 execute`/INSERT. Before export require exact info name, UUID/database ID, and `version: production`. Require each export to be a regular nonempty recognizable SQL file. Compute SHA-256 and atomically write/readback/fsync/rename `manifest.sha256.json` containing only binding, name/ID, version, relative filename, byte count, hash, and timestamps. Create COMPLETE only after exact-three verification. Failure writes sanitized FAILED metadata, never COMPLETE, and never auto-deletes partial/old backups. Add local-only `--verify-manifest <absolute-path>` with the same outside-repo and exact-three checks.

Create `scripts/d1-backup.test.mjs` with `node:test` and injected fake runner/filesystem. Test destination rejection, symlink/junction escape, exact configuration, child RTK argv/`shell:false`, identity mismatch, secret redaction, zero-byte output, tampered hash, exact-three success, and absence of execute/migration/restore/delete/`--command`.

Add `/backups/` to `.gitignore` as defense in depth. Update `package.json` to `2.0.2`; add `test:d1-backup`, `test:uat-live-safety`, and `test:operations`; include `test:operations` in `test:all`.

**C. Harden authenticated live UAT and ambiguous recovery**

Refactor `scripts/uat-live-realtime.mjs` with explicit `--live`, `--base-url`, `--branch`, `--env-file`, `--journal-dir`, `--self-test`, and `--cleanup-only`. Production mutation requires `--live`, HTTPS, allowlisted branch, absolute ACL-restricted journal directory outside repo, known UAT product, and authenticated cashier/owner. Allowlist-load only `UAT_PASSWORD`, `CLOUDFLARE_API_TOKEN`, and `CLOUDFLARE_ACCOUNT_ID` from process/env file. Require all three before active-session preflight or checkout because identity-verified D1 ambiguity recovery and cleanup verification must remain available once journaling can begin. Construct Wrangler child environments from only required OS execution keys (`PATH`, `PATHEXT`, `SystemRoot`, `TEMP`, `TMP`, `USERPROFILE`, `APPDATA`, `LOCALAPPDATA` when present) plus those three allowlisted credentials; do not inherit the full process environment. Redact every nonempty loaded value from all diagnostics. Never print password, API token, account ID, cookies, CSRF, headers, env values, or raw bodies.

After login but before journal or mutation, issue authenticated read-only `GET /api/sesi-toko?branch=<branch>&is_active=true&limit=2`. Require exactly one active session and record only its non-secret ID. Zero or multiple active sessions must pause execution with sanitized status `human_needed: active_store_session`, without opening/closing a store and without creating a journal. This is a blocking pre-commit gate: do not proceed to staging, commit, or push until a human confirms normal operations have opened exactly one session and the same read-only preflight passes on resume. Never POST/PATCH `/api/sesi-toko`.

Atomically journal `{base_url, branch, configured_database_name, configured_database_id, idempotency_key, status:'pending'}` before checkout. A prior unresolved journal blocks a new checkout. On successful response atomically add returned `transaction_id` and `buku_kas_id` before realtime waits. Realtime predicates must match branch/table/action and exact returned `transaction_id`, or exact returned `buku_kas_id` where the event carries ledger ID; a generic branch insert is insufficient for either client.

For a lost/ambiguous checkout response, retry the identical idempotent request once. If still ambiguous, do not call a runtime lookup API and do not deploy anything. Select the shard only through the fixed branch-to-binding map from `wrangler.pages.jsonc`, run identity verification with RTK/Wrangler `d1 info`, then run exactly one allowlisted read-only remote D1 SELECT of `buku_kas.transaction_id,id` by internally generated idempotency key and branch. Validate the key/branch against strict allowlists before constructing fixed SQL; reject arbitrary SQL, table, columns, database, config, or mutation verbs. Require zero or one row; multiple rows fail closed. This D1 execute mode exists only in the UAT recovery/verification helper, never in backup mode.

Cleanup runs in unsuppressed `finally` and in `--cleanup-only`. If journal lacks transaction ID, identity-verify then recover it using the same fixed D1 lookup. Owner DELETE the exact transaction when found. Afterwards prove all three independently: zero `transaksi_kasir` rows for transaction ID, zero `buku_kas` rows for returned ledger/transaction identity, and zero identity-verified D1 `buku_kas` rows for idempotency key. DELETE 404 is acceptable only when all three are definitively zero. Any failed/unknown/nonzero check retains the journal and exits nonzero with only safe cleanup instructions. Atomically mark `cleaned` only after proof.

Add `--self-test` using fake fetch/socket/RTK runner/filesystem. Cover missing Cloudflare token/account ID failing before active-session lookup and checkout; value redaction and minimal child environment; closed/multiple active session causing no journal/mutation; journal-before-write; exact realtime match and unrelated-event rejection; ambiguous response D1 identity match/mismatch; cleanup-only without transaction ID; successful 404-plus-three-zero cleanup; and 404 with any residue failing. Self-tests make zero network calls.

**D. Create truthful handover documents**

Write four concise Indonesian root documents without secrets:

- `OWNER-GUIDE.md`: actual login/branch/role boundaries, buka/tutup toko, products, cash, QRIS manual confirmation, receipt/reprint, reports, transaction review/delete, offline queue/sync, logout. State cashier requires active store session; owner can transact outside session with warning.
- `OPERATIONS-RUNBOOK.md`: daily checks, Worker health, 200/401 expectations, external backup/manifest commands, ACL, COMPLETE/FAILED, owner-selected retention, offline queue incidents, auth/realtime triage, evidence/escalation, credential rotation. Restore is destructive and out of automated scope; require separately approved recovery plan, exact target identity, maintenance window, backup validation, and post-restore proof.
- `HANDOVER-CHECKLIST.md`: GitHub, Cloudflare, domain, password manager, owner account, 2FA, least privilege, backup, candidate commit SHA, owner UAT, cleanup, later tag, credential rotation, support window, issue log, dated acceptance. Leave all human acceptance/tag items unchecked.
- `KNOWN-LIMITATIONS.md`: QRIS online/manual not gateway-confirmed; offline cash-only after online warm-up; IndexedDB pending must reach zero before site-data clearing; reports selected-branch only; cashier needs open store; browser/OS controls background sync; printing device-dependent; backup manual until external scheduler; current admin/owner inconsistencies.

**E. Separate technical gates, external backup, and live proof**

Run separately and stop on failure:

`rtk pnpm check`

`rtk pnpm lint`

`rtk pnpm test:d1-backup`

`rtk pnpm test:uat-live-safety`

`rtk pnpm test:all`

`rtk pnpm test:e2e:pos`

`rtk pnpm build`

`rtk git diff --check`

Create/resolve `D:\ZatiarasPOS-Backups`, prove it is outside repo, restrict inheritance and grant Full Control only to current Windows user and SYSTEM using RTK-prefixed ACL commands, then verify ACL without secrets. Run:

`rtk pnpm d1:backup -- --output-dir D:\ZatiarasPOS-Backups --env-file .env`

`rtk pnpm d1:backup -- --verify-manifest <returned-absolute-manifest-path>`

Require exact three production identities, nonempty SQL files, hashes, and COMPLETE. Never copy SQL/manifest into repo, summary, terminal, or Git.

Run read-only live probes:

`rtk curl -sS -o NUL -w "%{http_code}" https://zatiaraspos.pages.dev/`

`rtk curl -sS -o NUL -w "%{http_code}" https://zatiaraspos.pages.dev/login`

`rtk curl -sS -o NUL -w "%{http_code}" "https://zatiaraspos.pages.dev/api/produk?branch=samarinda"`

`rtk curl -sS -o NUL -w "%{http_code}" "https://zatiaraspos.pages.dev/api/pos/catalog?branch=samarinda"`

`rtk curl -sS -o NUL -w "%{http_code}" "https://zatiaraspos.pages.dev/api/realtime?branch=samarinda"`

`rtk curl -sS https://zatiaraspos-realtime.zulilmiihsn.workers.dev/health`

Require Pages 200/200, protected APIs 401/401/401, and exact healthy Worker JSON.

Use a new external ACL-restricted `D:\ZatiarasPOS-Backups\uat-journal` and run:

`rtk node scripts/uat-live-realtime.mjs --live --base-url https://zatiaraspos.pages.dev --branch samarinda --env-file .env --journal-dir D:\ZatiarasPOS-Backups\uat-journal`

This live gate must resolve before Stage F. If active store is closed/ambiguous, pause as blocking `human_needed`; do not treat skipped mutation as technical readiness, and do not stage, commit, or push. Resume only after a human independently opens the store through normal operations and the read-only preflight returns exactly one session. If mutation begins, staging/commit/push remain blocked until exact realtime correlation and three-way zero-residue cleanup pass. On failure run only the emitted RTK-prefixed cleanup-only command; unresolved journal/residue is a blocking failure and the candidate must not be published. Never run live browser, QRIS, store open/close, rate-limit, load, arbitrary write, restore, migration, or deploy in this automated plan.

**F. Secret-safe staged review, one source/docs commit, and candidate push**

Re-run status/diff checks. Stage only:

`rtk git add -- .gitignore package.json scripts/d1-backup.mjs scripts/d1-backup.test.mjs scripts/uat-live-realtime.mjs OWNER-GUIDE.md OPERATIONS-RUNBOOK.md HANDOVER-CHECKLIST.md KNOWN-LIMITATIONS.md`

Require `rtk git diff --cached --name-only` to equal exactly those nine files and run `rtk git diff --cached --check`. Before any full cached diff is printed, create/use an external non-repo scanner invoked with `rtk node <external-staged-secret-scanner>`. It must obtain staged added lines through an RTK-prefixed Git child, compare against env-file values and patterns for credential assignments, bearer tokens, private keys, JWTs, cookies, database dumps, and high-entropy keys, and output only PASS or finding type plus file/line number—never matched text/value. Any finding blocks review/commit.

Only after secret scan PASS, run `rtk git diff --cached --stat` and `rtk git diff --cached` for full review. Confirm no SQL, manifest, `.env`, root PLAN, quick-plan metadata, or unrelated file. Create exactly one atomic source/docs commit:

`rtk git commit -m "chore(release): harden backup and owner handover"`

Immediately capture `rtk git rev-parse HEAD` as immutable `RELEASE_COMMIT_SHA`, and require `rtk git show --name-only --format= <RELEASE_COMMIT_SHA>` to contain exactly the nine allowlisted files. Push with `rtk git push origin dev`, fetch, and require `origin/dev` contains that SHA.

Do not create or push `v2.0.2`. The GSD quick workflow may subsequently create a separate PLAN/SUMMARY metadata commit. Write literal `metadata_sha: pending_orchestrator` during executor output; only the orchestrator may replace it after that commit exists. Never redefine `RELEASE_COMMIT_SHA` as later `HEAD`. The eventual external tag after explicit owner approval must target the recorded source/docs SHA using `rtk git tag -a v2.0.2 <RELEASE_COMMIT_SHA> -m "ZatiarasPOS owner handover v2.0.2"` and `rtk git push origin v2.0.2`, only after fresh local/remote conflict checks. Those tag commands are documentation for later human-approved execution, not authorized in this automated run.

Do not run any Cloudflare deploy command. Planned files do not alter deployed routes, components, static assets, Worker source, bindings, migrations, or lockfile. Unexpected runtime changes stop for a new deployment decision.
  </action>
  <verify>
    <automated>
`rtk pnpm test:d1-backup`
`rtk pnpm test:uat-live-safety`
`rtk pnpm test:operations`
`rtk pnpm check`
`rtk pnpm lint`
`rtk pnpm test:all`
`rtk pnpm test:e2e:pos`
`rtk pnpm build`
`rtk git diff --check`
`rtk pnpm d1:backup -- --output-dir D:\ZatiarasPOS-Backups --env-file .env`
`rtk pnpm d1:backup -- --verify-manifest <returned-absolute-manifest-path>`
`rtk node scripts/uat-live-realtime.mjs --self-test`
`rtk git show --name-only --format= <RELEASE_COMMIT_SHA>`
`rtk git ls-remote --heads --tags origin`
    </automated>
  </verify>
  <done>Exact planned code/docs are tested, secret-scanned, committed once, and pushed as recorded RELEASE_COMMIT_SHA only after fresh external backup, read-only probes, exactly one active session, exact realtime correlation, and three-way zero-residue cleanup all pass; otherwise execution pauses/fails before publication. No tag/deploy/store-session mutation/restore/unrelated staging occurs.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repository to external backup storage | Raw production SQL must enter a restricted absolute directory that cannot resolve inside Git/workspace. |
| Local env to RTK/Wrangler child | Cloudflare/UAT credentials remain process-only and absent from argv/logs/artifacts. |
| Config to production D1 | Exact branch/binding/name/UUID/version must identify the intended shard before export or read-only recovery query. |
| Manifest to SQL files | Backup integrity depends on exact cardinality, byte counts, hashes, and atomic COMPLETE state. |
| UAT to production financial APIs/D1 | One authenticated write requires active-session preflight, exact realtime identity, durable journal, and three-way cleanup proof. |
| Git source commit to later metadata/tag | GSD metadata may advance HEAD; eventual release tag must still target recorded source/docs SHA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-1AM-01 | Spoofing | Production D1 target | mitigate | Exact config allowlist plus name/UUID/version identity before export and UAT lookup. |
| T-1AM-02 | Tampering | Backup/manifest | mitigate | Regular nonempty SQL, SHA-256, byte counts, atomic manifest/readback, COMPLETE after exact-three only. |
| T-1AM-03 | Repudiation | Backup/UAT/release | mitigate | Sanitized timestamps, identities, hashes, journal state, cleanup proof, RELEASE_COMMIT_SHA, metadata SHA. |
| T-1AM-04 | Information Disclosure | SQL/env/cookies/tokens | mitigate | External ACLs, allowlisted env, value redaction, suppressed-value secret scan before full diff, no raw artifacts in Git/summary. |
| T-1AM-05 | Denial of Service | Production | mitigate | Read-only backup/smoke, one bounded UAT only with existing active session, no load/rate-limit/deploy/store mutation. |
| T-1AM-06 | Elevation of Privilege | UAT cleanup/access | mitigate | Authenticated role/CSRF, owner exact deletion, fixed read-only recovery query, least privilege and 2FA handover. |
| T-1AM-07 | Tampering | Git/tag/unrelated work | mitigate | Pre-edit overlap check, exact stage allowlist, one source commit, separate metadata SHA, no tag until owner approval, no force. |

</threat_model>

<rollback_safety>

- Never invoke restore, migration, arbitrary D1 SQL, Time Travel restore, or production deletion except exact UAT cleanup.
- Backup mode permits only D1 info/export. UAT D1 execute permits fixed validated SELECT only after target identity verification.
- Never write/copy/stage/commit/summarize raw SQL, manifest contents, secrets, cookies, or tokens.
- Never delete old/partial backups automatically; failed runs lack COMPLETE.
- Never open/close a production store session. Closed/ambiguous session returns human_needed before journal/write.
- Never accept generic realtime branch events; both clients must correlate to returned transaction/ledger identity.
- Never accept DELETE 404 without transaction, ledger, and idempotency all definitively zero.
- Never reset, checkout, clean, stash, force-push, force-tag, or broadly stage.
- If source commit is pushed but later metadata advances HEAD, preserve RELEASE_COMMIT_SHA. Eventual tag targets it, never metadata HEAD.
- No tag is authorized until explicit owner approval. No Cloudflare deploy is authorized in this plan.

</rollback_safety>

<verification>

1. Preflight/fetch/baseline/tag/overlap checks happen before every planned edit.
2. Backup and UAT self-tests are network-free and cover all fail-closed paths.
3. Check, lint, operations, full tests, local E2E, build, and diff checks pass separately.
4. Fresh external backup proves exact three production identities/files/hashes and COMPLETE.
5. Read-only live probes return expected 200/401/health.
6. Active-session preflight must return exactly one session and live UAT must prove exact two-client event correlation plus three-way zero residue before staging/commit/push; zero/multiple sessions pause as blocking human_needed.
7. Exact allowlist is staged; suppressed-value secret scan passes before full cached diff output.
8. One source/docs candidate commit is pushed and recorded as RELEASE_COMMIT_SHA.
9. Later GSD metadata SHA is kept distinct; no v2.0.2 tag and no Cloudflare deployment occur.

</verification>

<human_needed>

Automated quick execution ends at technical candidate readiness. Owner UAT and acceptance happen after the quick workflow, outside executor tasks.

- Owner reviews the four documents and tests login/roles, existing store-session flow, cash, QRIS manual confirmation, receipt/reprint, selected-branch report, offline cash sync, and access transfer.
- Every owner-created UAT transaction must be recorded, deleted, and proven absent before approval. Pending queue must be zero before clearing site data.
- Owner explicitly approves or reports failed steps. Never infer approval.
- Only after explicit approval: re-fetch tags, verify `v2.0.2` absent locally/remotely, verify recorded `RELEASE_COMMIT_SHA` still names the exact nine-file source/docs commit, then create/push annotated `v2.0.2` against that SHA—not current HEAD or later GSD metadata commit.
- If owner does not approve, status remains `human_needed`; no tag is created.

</human_needed>

<success_criteria>

- Backup is external-only, read-only, exact-three-shard, identity-checked, SHA-256 manifested, and tested.
- Secrets/raw SQL never enter Git, logs, docs, summary, or chat.
- Four handover documents reflect actual application behavior and leave acceptance/tag unchecked.
- Technical gates, fresh backup, read-only smoke, exactly-one-session preflight, exact live correlation, and proven cleanup complete before candidate publication.
- Exact atomic source/docs candidate commit is pushed to origin/dev and recorded independently from later metadata.
- No tag, Cloudflare deploy, store-session mutation, restore, force operation, or unrelated staging occurs.
- Final state is technical candidate ready plus `human_needed` owner acceptance.

</success_criteria>

<output>
Create `.planning/quick/260730-1am-harden-production-backup-outside-reposit/260730-1am-SUMMARY.md` after the task with sanitized evidence, `release_commit_sha: <RELEASE_COMMIT_SHA>`, literal `metadata_sha: pending_orchestrator`, backup manifest path/hash only, live result (`passed`, `human_needed`, or `blocked`), and explicit `owner_acceptance: human_needed`. The executor must not predict or record a future metadata SHA; the orchestrator may replace `pending_orchestrator` only after its separate metadata commit exists. Never include SQL/manifest contents, secrets, cookies, tokens, CSRF, raw headers, or matched secret-scan values. Do not stage PLAN/SUMMARY with the source/docs commit. Do not tag.
</output>
